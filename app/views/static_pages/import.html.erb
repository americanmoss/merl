<% provide(:title, 'Import') %>
<div class="row">
	<div class="small-12 large-centered columns text-center">
		<%= image_tag("merl.png") %>
	</div>
	<div class="small-12 large-centered">
	<p>Use the interface below to upload data to the MERL platform. Please note that this is <strong>NOT</strong> recommended after initial deployment, as it can cause duplicates to be created in the database. Be sure to <%= link_to "download a backup", export_path %> before proceeding.</p>
	</div>
	<div class="small-8 large-centered columns text-center">
		<%= form_tag import_users_path, multipart:true do %>
			<%= file_field_tag :file %>
			<%= submit_tag :import, class: "button secondary" %>
		<% end %>
	</div>
</div>